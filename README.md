# Vansono - Modern Voice & Video Messenger

Современный мессенджер, ориентированный на высококачественные голосовые и видео-звонки с использованием WebRTC технологии.

## 🚀 Особенности

- **P2P соединения** через WebRTC для минимальной задержки
- **Высококачественные аудио и видео звонки**
- **Темная тема** с современным дизайном
- **Система аутентификации** с email верификацией
- **Управление контактами** и поиск пользователей
- **Онлайн статусы** в реальном времени
- **Адаптивный дизайн** для десктопа и мобильных устройств

## 🛠 Технологический стек

### Backend
- **Node.js** с Express.js
- **PostgreSQL** для хранения данных пользователей
- **Redis** для кэширования и сессий
- **Socket.IO** для WebRTC сигналинга
- **JWT** для аутентификации
- **Nodemailer** для отправки email

### Frontend
- **React 18** с хуками
- **Vite** для быстрой сборки
- **Tailwind CSS** для стилизации
- **Zustand** для управления состоянием
- **Socket.IO Client** для WebSocket соединений
- **WebRTC API** для P2P соединений

## 📋 Требования

- Node.js 16+ 
- PostgreSQL 12+
- Redis 6+
- Современный браузер с поддержкой WebRTC

## 🔧 Установка и запуск

### 1. Клонирование и установка зависимостей

```bash
# Установка зависимостей сервера
npm install

# Установка зависимостей фронтенда
cd frontend
npm install
cd ..
```

### 2. Настройка базы данных

Создайте базу данных PostgreSQL:

```sql
CREATE DATABASE vansono;
```

### 3. Настройка Redis

Убедитесь, что Redis запущен на порту 6379:

```bash
redis-server
```

### 4. Настройка переменных окружения

Скопируйте файл конфигурации и настройте переменные:

```bash
cp server/config.env server/.env
```

Отредактируйте `server/.env`:

```env
# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vansono
DB_USER=postgres
DB_PASSWORD=your_password

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Email Configuration (Gmail SMTP)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# CORS Configuration
CORS_ORIGIN=http://localhost:5173
```

### 5. Настройка email (опционально)

Для отправки email с кодами верификации:

1. Включите двухфакторную аутентификацию в Gmail
2. Создайте пароль приложения
3. Укажите email и пароль приложения в `.env`

### 6. Запуск приложения

```bash
# Запуск сервера (в одном терминале)
npm run dev

# Запуск фронтенда (в другом терминале)
cd frontend
npm run dev
```

Приложение будет доступно по адресам:
- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:3000

## 📱 Использование

### Регистрация
1. Перейдите на страницу регистрации
2. Введите email и пароль
3. Проверьте email и введите код верификации
4. Завершите регистрацию

### Добавление контактов
1. Используйте поиск для поиска пользователей
2. Нажмите кнопку "+" для добавления в контакты

### Звонки
1. Выберите контакт из списка
2. Нажмите кнопку "Voice Call" или "Video Call"
3. Дождитесь ответа или ответьте на входящий звонок

## 🔒 Безопасность

- Пароли хешируются с помощью bcrypt
- JWT токены для аутентификации
- CORS настройки для безопасности
- Rate limiting для предотвращения спама
- Helmet для защиты заголовков

## 🌐 WebRTC

Приложение использует WebRTC для P2P соединений:
- **STUN серверы** для NAT traversal
- **ICE кандидаты** для установления соединения
- **SDP offer/answer** для обмена медиа-данными
- **Signaling сервер** через Socket.IO

## 📁 Структура проекта

```
vansono/
├── server/
│   ├── server.js          # Основной сервер
│   └── config.env         # Конфигурация
├── frontend/
│   ├── src/
│   │   ├── pages/         # Страницы приложения
│   │   ├── stores/        # Zustand stores
│   │   ├── App.jsx        # Главный компонент
│   │   └── main.jsx       # Точка входа
│   ├── package.json
│   └── vite.config.js
├── package.json
└── README.md
```

## 🐛 Устранение неполадок

### Проблемы с WebRTC
- Убедитесь, что браузер поддерживает WebRTC
- Проверьте настройки микрофона и камеры
- Для локальной разработки может потребоваться HTTPS

### Проблемы с базой данных
- Проверьте подключение к PostgreSQL
- Убедитесь, что база данных создана
- Проверьте права пользователя

### Проблемы с Redis
- Убедитесь, что Redis запущен
- Проверьте подключение к Redis

## 🚀 Развертывание

Для продакшена:

1. Настройте HTTPS для WebRTC
2. Используйте продакшен базу данных
3. Настройте продакшен Redis
4. Обновите CORS настройки
5. Используйте сильные секретные ключи

## 📄 Лицензия

ISC License

## 🤝 Вклад в проект

Приветствуются любые улучшения и исправления!

---

**Vansono** - современный мессенджер для качественного общения 🎯
